@layout('main')

@section('content')
<h1>{{ title }}</h1>
<ul>
    @each(post in posts)
        <li>{{ post.title}}</li>
        {{--  <li>{{ post.body}}</li>  --}}
    @endeach
    
</ul>

@endsection
